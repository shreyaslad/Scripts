#!/usr/bin/env node

console.log("\nWelcome to the helper tool for remembering where all your scripts are.\n");
console.log("Global Directory Instructions:");
console.log("On Mac, move your .sh files to the folder /usr/local/bin and then you can just type in the filename");
console.log("On Windows, type 'path' into the search bar, click 'Edit the system environment variables', click the 'Environment Variables' button, then click 'New', and type in the directory where these files are.\n\n");
console.log("1. File Options");
console.log("   - File Name: movepls.sh");